export class Author {
    public id: string;
    public name: string;
    public number: string;

    constructor(name, number) {
        this.name = name;
        this.number = number;
    }
}